# Memory

Long-lived context the agent should weigh when planning.

- Scope of the current task: close server-action authentication gaps in 8 functions across 3 files (~70 line changes). Do not touch unrelated files.
- The Firebase rewrite previously caused production data loss; require explicit confirmation before any destructive write to production routing.
